#!/usr/bin/env ruby

require_relative '../downloader'

begin
	dl = ShowDownloader::Downloader.new
	dl.download_single_show(ARGV.join(" "))

rescue Interrupt
	puts "Interrupt signal detected. Exiting..."
end