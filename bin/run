#!/usr/bin/env ruby

require_relative '../downloader'

begin
	dl = ShowDownloader::Downloader.new(*ARGV)
	dl.run(ARGV.include?("-a"))

rescue Interrupt
	puts "Interrupt signal detected. Exiting..."
end